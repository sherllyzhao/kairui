---
import {requestData} from "../assets/js/jzt_common.ts";
import Banner from "../components/Banner.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

interface Props {
  /**
   * 滚动动画模式。
   * once（默认）：进场播一次即锁定，不再退场。
   * toggle：进出视口反复播放 —— 仅适用于用 transition 实现动画的页面（如首页）。
   *   用 animation 的页面切勿使用 toggle：类被摘掉时 animation 整个失效，
   *   元素会瞬间复位造成闪烁，且位移会反馈影响可见性判定导致反复重播。
   */
  revealMode?: 'once' | 'toggle';
}

const {revealMode = 'once'} = Astro.props;

let siteInfo = {
  title: '山西凯瑞工业发展有限公司',
  keywords: '山西凯瑞工业发展有限公司',
  description: '山西凯瑞工业发展有限公司',
  icon: ''
};
try {
  siteInfo = await requestData('site');
} catch (error) {
  // 加载失败时保留上面的默认值兜底
  console.error('[Layout] 获取站点信息失败:', error);
}
---

<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8"/>
	<title>{siteInfo.title || '山西凯瑞工业发展有限公司'}</title>
	<meta name="description" content={siteInfo.description || '山西凯瑞工业发展有限公司'}>
	<meta name="keywords" content={siteInfo.keywords || '山西凯瑞工业发展有限公司'}>
	<meta name="viewport" content="width=device-width"/>
	<link rel="icon" href={siteInfo.icon || './favicon.ico'}/>
	<link rel="stylesheet" href="/css/animate.min.css">
	<link rel="stylesheet" href="/css/common.css">
	<link rel="stylesheet" href="/css/header.css">
	<link rel="stylesheet" href="/css/banner.css">
	<link rel="stylesheet" href="/css/footer.css">
	<!-- 必须在 body 渲染前打上 motion-ready，否则元素会先以可见状态绘制一帧再被隐藏，产生闪烁 -->
	<script is:inline>document.documentElement.classList.add('motion-ready');</script>
	<script is:inline src="/js/jquery-4.0.0.min.js"></script>
</head>
<body>
<Header/>
<slot/>
<Footer/>
<script is:inline src="/js/common.js"></script>
<script is:inline src="/js/scroll-reveal.js"></script>
<script is:inline define:vars={{revealMode}}>
  ScrollReveal.init({
    mode: revealMode,
    selector: '.reveal-on-scroll',
    // 入/退场位移约 110px。离场加 140px 缓冲，避免页面顶部/底部反复抖动。
    visibility: {top: 0.88, bottom: 0.12},
    toggleExitOffset: 140
  });
</script>
</body>
</html>

<style>
    html,
    body {
        margin: 0;
        width: 100%;
        height: 100%;
    }
</style>
